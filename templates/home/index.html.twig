{% extends 'base.html.twig' %}

{% block title %}!{% endblock %}

{% block body %}

<div class="flex flex-row h-screen w-screen">
    <div class="flex flex-col items-center justify-center w-1/2">
        <a href="{{ path('app_catalog') }}" class="h-screen">
            <img src="{{ asset('./image/la-tendance.webp') }}" alt="logo" class="h-screen">
        </a>
    </div>
    <div class="flex flex-col items-center justify-center w-1/2">
        <a href="{{ path('app_catalog') }}" class="h-screen">
            <img src="{{ asset('./image/l-obsession.webp') }}" alt="logo" class="h-screen">
        </a>
    </div>
    <div class="flex flex-col items-center justify-center w-1/2">
        <a href="{{ path('app_catalog') }}" class="h-screen">
            <img src="{{ asset('./image/le-desir.webp') }}" alt="logo" class="h-screen">
        </a>
    </div>

</div>

{% endblock %}
